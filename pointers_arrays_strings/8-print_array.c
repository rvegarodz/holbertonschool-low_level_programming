#include "main.h"
#include <stdio.h>
/**
 *
 *
 *
 */
void print_array(int *a, int n)
{
	int y;

	for (y = 0; y <= 0; y++)
	{
		if (y < n)
		{
			printf("%d", a[y]);
			printf(", ");
		}
		else (y = n)
		{
			printf("%d\n", a[y]);
		}
	}

}
