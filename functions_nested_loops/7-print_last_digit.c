#include "main.h"
/**
 * f
 * f
 * f
 * f
 */
int print_last_digit(int x)
{
	int digit;

	if (x < 0)
	{
		x = x * -1
		digit = x % 10;
		return (digit);
	}
	else
	{
		digit = x % 10;
		return (digit);
	}
}
